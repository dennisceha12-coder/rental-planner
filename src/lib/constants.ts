/** Default uurtarief manuren (EUR) — voorbeeld in formulier. */
export const DEFAULT_HOURLY_RATE = 45;

/** Default transportvergoeding (EUR per km). */
export const DEFAULT_TRANSPORT_RATE_PER_KM = 0.65;
